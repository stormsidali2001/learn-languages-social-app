import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'front-end-learn-languages-social-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
